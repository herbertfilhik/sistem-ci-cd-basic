name: CI/CD Pipeline Basic

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Node.js
      uses: actions/setup-node@v1
      with:
        node-version: '14'
    - name: Install dependencies
      run: npm install
      working-directory: sistem-ci-cd-basic/meu-app-express
    - name: Run tests
      run: npm test
      working-directory: sistem-ci-cd-basic/meu-app-express
